#
#  M32R/m32rsim Support 
#
#  $Id$
#

runBSP()
{
  ${RUN} ${1}
}

checkBSPFaults()
{
  return 0
}

bspLimit()
{
  testname=$1
  case ${tname} in
    *stackchk*)limit=5 ;;
    *fatal*)   limit=1 ;;
    *psxtime*) limit=180 ;;
    *)         limit=60 ;;
  esac
  return ${limit}
}
